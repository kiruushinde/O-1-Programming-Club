#include <iostream>
using namespace std;

int main()
{

    // char name[] = {'a', 'b', 'c', '\0', 'd', 'e'};

    // int size = sizeof(name) / sizeof(char);

    // cout << "size of array is : " << size << endl;
    // for (int i = 0; i < size; i++)
    // {
    //     cout << name[i];
    // }

    cout << name;
    return 0;
}